

fun main (args: Array<String>)
{
    val realName = "Juan José" // Se usa VAL para variables inmutables (constantes)
    var alias = "JJ" // Se usa VAR para variables mutables

    println("Hi $alias!! ($realName)") // Concatenación con operador $
}